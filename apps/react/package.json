{
  "name": "@gredor/react-app",
  "version": "0.0.0",
  "description": "React + TanStack Start rewrite of the Gredor frontend (in progress).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate -w @gredor/domain && vite build",
    "preview": "vite preview --port 4174",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@gredor/domain": "*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "sass-embedded": "^1.98.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.16"
  }
}
